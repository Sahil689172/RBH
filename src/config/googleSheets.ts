/**
 * Google Apps Script Web App URL for order submissions.
 * Set VITE_GOOGLE_SCRIPT_URL in .env (see .env.example).
 */
export const GOOGLE_SCRIPT_URL = import.meta.env.VITE_GOOGLE_SCRIPT_URL ?? '';
